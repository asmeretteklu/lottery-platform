# Vite Environment Variables (prefixed with VITE_)
VITE_API_URL= # Base URL of the Lottery API (e.g., http://localhost:3000)
VITE_ADMIN_ROLE_SECRET= # Optional secret for admin panel bypass if needed in dev
